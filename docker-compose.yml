services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-code-bug-classifier:latest
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      MODEL_DIR: /app/models
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
