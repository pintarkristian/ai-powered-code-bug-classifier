# Models Directory

This folder is reserved for trained model artifacts.

Suggested artifacts after implementation:

```text
models/tensorflow_baseline.keras
models/codebert-bug-classifier/
```

Do not commit large model files. Store them in an artifact registry, model hub, or release asset instead.
